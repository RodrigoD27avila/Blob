#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char **argv)
{
	vector<int> myvec;
	myvec.push_back(10);
	myvec.push_back(11);
	myvec.push_back(12);
	myvec.push_back(13);
}

