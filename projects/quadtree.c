#include <stdio.h>
#include <stdlib.h>

int insert(int x, int y)
{
    int error = 0;

    if (x < 100 && y < 100) {
        (100*y)+x
    }
    else {
        error = -1;
    }

    return error;
}

int main()
{
    short *quadtree = calloc(262145, sizeof(short));
}
