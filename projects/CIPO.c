#include <stdio.h>

typedef struct {
 int u, v
} PATH;

int  graph[1000][1000];
PATH set[1000000];


int main()
{
    int n, m, u, v, c, i;
    scanf("%d %d", &n, &m);
    for (i=0; i<m; i++) {
        scanf("%d %d %d", &u, &v, &c);

    }
    return 0;
}
